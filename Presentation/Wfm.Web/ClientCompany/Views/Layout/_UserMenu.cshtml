@using Wfm.Core
@{
    Layout = null;
    var workContext = EngineContext.Current.Resolve<IWorkContext>();
}
<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    <i class="fa fa-user"></i>
    <span class="hidden-xs hidden-sm hidden-md hidden-lg">@workContext.CurrentAccount.Username</span>
</a>
<ul class="dropdown-menu">
    <li class="user-header">
        @*<img src="../../dist/img/user2-160x160.jpg" class="img-circle" alt="User Image">*@
        <p>
            @workContext.CurrentAccount.Username
            <br />
            @workContext.CurrentAccountRole.AccountRoleName
            <br /><br />
            <small>Last login @@ @workContext.CurrentAccount.LastLoginDateUtc.Value.ToShortDateString()</small>
        </p>
    </li>
    @*<li class="user-body">
        <div class="row">
            <div class="col-xs-4 text-center">
                <a href="#">Followers</a>
            </div>
            <div class="col-xs-4 text-center">
                <a href="#">Sales</a>
            </div>
            <div class="col-xs-4 text-center">
                <a href="#">Friends</a>
            </div>
        </div>
    </li>*@
    <li class="user-footer">
        <div class="pull-left">
            <a class="btn btn-default btn-flat" href='@Url.Action("Details", "Account")'><i class="fa fa-pencil"></i> @T("Admin.Profile").Text</a>
        </div>
        <div class="pull-right">
            <a class="btn btn-default btn-flat" href='@Url.Action("SignOut", "Account")'><i class="fa fa-sign-out"></i> @T("Admin.Accounts.Account.Logout").Text</a>
        </div>
    </li>
</ul>
