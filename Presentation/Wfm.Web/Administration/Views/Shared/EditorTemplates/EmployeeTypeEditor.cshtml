@model int
@{ var isEnabled = ViewData["isEnabled"] != null ? (bool)ViewData["isEnabled"] : true; }

@(Html.Kendo().DropDownListFor(m => m)
        //.OptionLabel("Select a employee type...")
        .DataTextField("Text")
        .DataValueField("Value")
        .DataSource(dataSource => dataSource.Read(read => read.Action("GetAllEmployeeTypes", "Common").Type(HttpVerbs.Post)).ServerFiltering(true))
        .Enable(isEnabled)
)
