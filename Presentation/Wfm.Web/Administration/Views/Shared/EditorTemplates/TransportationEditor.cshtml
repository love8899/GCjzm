@(Html.Kendo().DropDownListFor(m => m)
        //.AutoBind(false)
            .OptionLabel("--Select--")
        .DataTextField("Text")
        .DataValueField("Value")
        .DataSource(dataSource =>
        {
            dataSource.Read(read => read.Action("GetTransportationsAsSelectList", "Common"));
            //.ServerFiltering(true);
        })
)
@Html.ValidationMessageFor(m => m)