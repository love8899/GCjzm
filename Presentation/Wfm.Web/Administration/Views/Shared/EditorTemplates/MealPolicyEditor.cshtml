@model int

@(Html.Kendo().DropDownListFor(m => m)
        .AutoBind(false)
        .OptionLabel("None")
        .DataTextField("Text")
        .DataValueField("Value")
        .DataSource(dataSource =>
        {
            dataSource.Read(read => read.Action("GetCascadeMealPolicies", "Common").Data("filterPolicy"))
                    .ServerFiltering(true);
        })
        .CascadeFrom("CompanyId")
)
