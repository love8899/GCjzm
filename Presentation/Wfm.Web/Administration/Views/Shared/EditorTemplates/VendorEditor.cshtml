@model int
@{ var isEnabled = ViewData["isEnabled"] != null ? (bool)ViewData["isEnabled"] : true; }

@(Html.Kendo().DropDownListFor(m => m)
        //.OptionLabel("Select a vendor...")
        .DataTextField("Text")
        .DataValueField("Value")
        .DataSource(dataSource => dataSource.Read(read => read.Action("GetAllVendors", "Common").Type(HttpVerbs.Post)).ServerFiltering(true))
        .Enable(isEnabled)
)
