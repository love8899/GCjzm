@(Html.Kendo().DropDownListFor(m => m)
        //.AutoBind(false)
            .OptionLabel("--Select--")
        .DataTextField("Text")
        .DataValueField("Text")
        .DataSource(dataSource =>
        {
            dataSource.Read(read => read.Action("GetIntersectionsAsSelectList", "Common"));
            //.ServerFiltering(true);
        })
)
@Html.ValidationMessageFor(m => m)
