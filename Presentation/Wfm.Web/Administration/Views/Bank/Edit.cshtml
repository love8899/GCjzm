@model BankModel

@{
    //page title
    ViewBag.Title = T("Admin.Configuration.Bank.EditBankDetails").Text;
}

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "country-form" }))
{
    <div class="section-header">
        <div class="title">
            @T("Admin.Configuration.Bank.EditBankDetails") @Html.ActionLink("(" + T("Admin.Configuration.Bank.BackToList") + ")", "Index")
        </div>
        <div class="options">
            <input type="submit" name="save" class="k-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="k-button" value="@T("Admin.Common.SaveContinue")" />
            @Html.ActionLink(T("Admin.Common.Cancel").Text, "Index", null, new { @class = "k-button" })
        </div>
    </div>

    @Html.Partial("_CreateOrUpdate", Model)
}
