@model CompanyClockDeviceModel

@{
    //page title
    //ViewBag.Title = T("Admin.TimeClocks.CompanyClockDevice.AddNew").Text;
}

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "companyclockdevice-form" }))
{
    <div class="section-header">
        <div class="title">
            @T("Admin.ClockDevice") @Html.ActionLink("(" + T("Admin.TimeClocks.CompanyClockDevice.BackToList") + ")", "Index")
        </div>
        <div class="options">
            <input type="submit" name="save" class="k-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="k-button" value="@T("Admin.Common.SaveContinue")" />
        </div>
    </div>


    @Html.Partial("_CreateOrUpdate", Model)
}
