@using System.Drawing
@using System.Web.UI.WebControls
@using MvcReportViewer

@{
    var settings = new ControlSettings
    {
        SizeToReportContent = true,
        ShowParameterPrompts = false,
        ShowFindControls = true,
        BackColor = Color.AliceBlue,
        KeepSessionAlive = true,
        //Width = new Unit("100%"),
        //Height = new Unit("800px"),
        AsyncRendering = false
    };
}

@Html.MvcReportViewer(
            "/WFMReports/MarkupReport",
    new { StartDate = ViewBag.StartDate, EndDate = ViewBag.EndDate, CompanyId = ViewBag.CompanyIds, FranchiseId = ViewBag.VendorId, SelectedBatchId=0 },
    new { style = "border: none ; height: 100%; width: 100%;min-height: 600px" }
).ControlSettings(settings)


